const NAV_ITEMS = [
  { id: "dashboard",   label: "Dashboard",        icon: "🏠" },
  { id: "vehicles",    label: "Vehicles",          icon: "🚗" },
  { id: "trips",       label: "Trip Dispatcher",   icon: "🗺️" },
  { id: "maintenance", label: "Maintenance",        icon: "🔧" },
  { id: "fuel",        label: "Fuel & Expenses",   icon: "⛽" },
  { id: "drivers",     label: "Drivers",           icon: "👤" },
  { id: "analytics",   label: "Analytics",         icon: "📊" },
];

export default function Sidebar({ activePage, setActivePage, user, onLogout }) {
  return (
    <aside className="w-64 bg-gray-900 border-r border-gray-800 flex flex-col h-screen sticky top-0 shrink-0">

      {/* Logo */}
      <div className="p-5 border-b border-gray-800">
        <div className="flex items-center gap-3">
          <div className="w-9 h-9 bg-cyan-500 rounded-xl flex items-center justify-center text-black font-black text-lg">
            F
          </div>
          <span className="text-xl font-black text-white tracking-tight">FleetFlow</span>
        </div>
      </div>

      {/* User Info */}
      <div className="p-4 border-b border-gray-800">
        <div className="text-white font-bold text-sm capitalize">{user.name}</div>
        <div className="text-cyan-400 text-xs mt-0.5">{user.role}</div>
      </div>

      {/* Navigation */}
      <nav className="flex-1 p-3 overflow-y-auto">
        {NAV_ITEMS.map((item) => (
          <button
            key={item.id}
            onClick={() => setActivePage(item.id)}
            className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium mb-1 transition-all text-left ${
              activePage === item.id
                ? "bg-cyan-500 bg-opacity-20 text-cyan-400 border border-cyan-500 border-opacity-40"
                : "text-gray-400 hover:bg-gray-800 hover:text-white"
            }`}
          >
            <span className="text-base">{item.icon}</span>
            <span>{item.label}</span>
          </button>
        ))}
      </nav>

      {/* Logout */}
      <div className="p-4 border-t border-gray-800">
        <button
          onClick={onLogout}
          className="w-full text-sm text-gray-500 hover:text-red-400 transition-colors text-left px-3 py-2 rounded-xl hover:bg-gray-800"
        >
          🚪 Sign Out
        </button>
      </div>
    </aside>
  );
}
